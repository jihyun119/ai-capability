# Track 3 5턴 평가 플로우 설계

상태: 초안
전제 문서: 1_평가축_조작적정의.md, 2_LLM_Judge_루브릭.md
원칙: 기능(move) 기반 평가 — 턴 번호는 안내용 가이드일 뿐, 채점 기준이 아니다.

---

## 1. 전체 플로우

```text
[시작]
  │
  ▼
① 시나리오 제시 ──── 시나리오 정의 + 평가 기준 고정값 로드
  │
  ▼
② 5턴 대화 루프 (턴마다 반복)
  │   사용자 발화
  │     ├─ 실시간 코드 체크 (안전성·유효성)   ← 통과 못하면 재입력 요청, 턴 미소모
  │     └─ AI 응답 생성
  │   종료 조건: 5턴 소진 or 사용자 조기 완료 선언
  │
  ▼
③ 대화 종료 → 평가 입력 패키징
  │   사용자 발화 / AI 응답 / 최종 결과물 / 시나리오 정의
  │
  ├──────────────────────────────┐
  ▼                              ▼
④-A 코드화 일괄 평가          ④-B LLM Judge 평가
  (체크리스트 20점)             (루브릭 파이프라인 Step 1~4)
  │                              │
  └──────────────┬───────────────┘
                 ▼
⑤ 점수 합산 + 신뢰도 게이트
  │   confidence low → 리포트에 근거 부족 표기
  ▼
⑥ 결과 리포트 생성 → 학습 미션 매핑 (기획안 §10)
```

---

## 2. 단계별 상세

### ① 시나리오 제시

사용자에게 보여주는 것:

- 업무 시나리오 (상황, 역할, 가용 정보, 제약)
- 미션: "5턴 안에 AI와 함께 실무에 쓸 수 있는 결과물을 만드세요"
- 턴별 권장 흐름은 **힌트로만** 노출 (1턴: 문제 공유 → ... → 5턴: 최종화). 강제하지 않음

시스템이 로드하는 것:

- 시나리오별 평가 기준 고정값: 축별 4점 앵커의 시나리오 맞춤 버전 (Judge 임의 기준 채점 방지, 기획안 §8)
- 시나리오에 포함된 정보 목록 (축2 "주어진 정보 누락" 감점 판정용)

### ② 5턴 대화 루프

턴 정의: 사용자 발화 1회 + AI 응답 1회 = 1턴.

실시간 코드 체크 (턴 소모 전 게이트):

| 체크 | 동작 |
|---|---|
| 개인정보·부적절 입력 | 차단, 재입력 요청 (턴 미소모) |
| 공백·무의미 입력 (5자 미만 등) | 재입력 요청 (턴 미소모) |
| 시나리오와 전혀 무관한 입력 | 1회 경고 후 진행 허용 (평가에는 반영) |

종료 조건:

- 5턴 소진 (기본)
- 사용자 조기 완료 선언 ("이 결과물로 제출") — 기획안의 "최대 5턴" 반영. 미사용 턴은 감점하지 않되, 기능 커버리지 누락이 있으면 해당 축에서 자연 감점

최종 결과물 확정: 사용자가 마지막으로 받은 AI 응답 중 결과물에 해당하는 부분. 조기 종료 시 사용자가 "최종본"으로 지정한 응답.

### ③ 평가 입력 패키징

```json
{
  "scenario_id": "...",
  "turns": [{"n": 1, "user": "...", "assistant": "..."}],
  "final_output": "...",
  "early_finish": false,
  "turn_count": 5
}
```

### ④-A 코드화 일괄 평가 (20점 체크리스트)

루브릭 §8 미해결 항목의 구체화. 의미 판단 없이 관찰 가능한 신호만 체크.

| # | 항목 | 점수 | 신호 예 |
|---|---|---|---|
| 1 | 대화 완주 (5턴 또는 조기 완료 선언) | 4 | turn_count, early_finish |
| 2 | 발화 유효 길이 (턴 평균이 임계값 이상) | 3 | 문자 수 (길수록 가점 아님, 임계 통과만 확인) |
| 3 | 출력 형식 요청 표현 존재 | 3 | "표로", "포함해서", "형식" 등 |
| 4 | 검증 관련 표현 존재 | 4 | "지적해줘", "누락", "허점", "검토 기준" 등 |
| 5 | 후속 개입 표현 존재 (2턴 이후) | 3 | AI 답변 일부 인용, 선택·배제 표현 |
| 6 | 구조화 신호 존재 | 3 | 줄바꿈 구획, 구분자, 번호 매김 |

- 역할: 점수 20%를 구성하는 동시에 **Judge 결과 교차 검증**에 사용. 예: 코드가 검증 표현을 감지했는데 Judge 축7이 0점이면 재평가 플래그.

### ④-B LLM Judge 평가

루브릭 문서의 파이프라인 그대로 실행:

```text
Step 1. 기능 태깅 (M1~M5) + 순서 타당성
Step 2. 축1~8 채점 (0~4, 근거 인용)
Step 3. 델타 점수 (1턴 기대 결과물 vs 최종 결과물)
Step 4. 신뢰도 산출
```

운영 권장:

- Step 1과 Step 2~3을 별도 호출로 분리 (태깅 결과를 채점 프롬프트의 입력으로 제공 → 채점 일관성 향상)
- 동일 입력 2회 채점 후 축별 점수 차이가 2점 이상이면 3차 채점으로 중재 (파일럿 단계에서만)

### ⑤ 점수 합산 + 신뢰도 게이트

```text
종합(100) = 프로세스 50 (축1~7 평균×12.5)
          + 발전 15 (델타×3.75)
          + 결과 15 (축8×3.75)
          + 코드 체크리스트 20
```

신뢰도 게이트:

- confidence high/medium → 정상 리포트
- confidence low → 종합 점수 대신 "평가 근거 부족" 리포트, 재도전 유도
- 코드-Judge 불일치 플래그 → 해당 축 재평가 후 리포트 생성

### ⑥ 리포트 생성 → 학습 미션 매핑

리포트 구성은 기획안 §9 그대로. 추가 규칙:

- "가장 좋은 개입 / 놓친 개입"은 Judge 출력의 best_intervention / missed_intervention 사용
- 최저 점수 축 → 기획안 §10 매핑 테이블로 학습 미션 추천 (동점 시 검증 유도 > 상호작용 조율 > 맥락 제공 > 출력 설계 순 우선)

---

## 3. 엣지 케이스 처리

| 케이스 | 처리 |
|---|---|
| 1턴만 하고 이탈 | 평가 미수행, "이어서 진행" 유도 |
| 2~4턴 후 이탈 (조기 완료 선언 없음) | confidence low로 평가, 점수 비공개·피드백만 제공 |
| 첫 턴 만능 요청 후 단순 진행 반복 | 정상 평가 (축4·축6에서 자연 감점, 루브릭 공통 규칙 3·4) |
| AI 응답 오류·품질 저하 | 해당 턴 재생성 허용 (사용자 점수에 AI 품질 미반영 원칙 유지) |
| 동일 발화 반복 | 코드 체크에서 감지, Judge에 플래그 전달 |

---

## 4. 미해결 항목 (다음 작업)

- 시나리오 풀 설계 + 시나리오별 4점 앵커 작성
- 코드 체크 항목의 키워드 사전·임계값 정의
- Judge 프롬프트 실작성 (루브릭 → 시스템 프롬프트 변환)
- 파일럿: 채점 일관성(2회 채점 일치율) 측정 후 앵커 보정
