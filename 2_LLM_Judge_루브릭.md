# Track 3 LLM Judge 루브릭

상태: 초안
전제 문서: 1_평가축_조작적정의.md
평가 방식: 기능(move) 기반 평가 — 턴 위치가 아니라 필수 기능의 수행 여부와 품질을 채점한다.

---

## 0. Judge 평가 파이프라인

```text
입력: 사용자 5턴 발화 + AI 응답 + 최종 결과물 + 시나리오 정의

Step 1. 기능 태깅      각 사용자 발화에 수행된 기능(move) 라벨링
Step 2. 축별 채점      8개 평가축을 0~4점으로 채점 (근거 인용 필수)
Step 3. 발전 점수      1턴 기대 결과물 대비 최종 결과물의 개선 폭 채점
Step 4. 신뢰도 산출    대화 완결성·근거 충분성 기반 confidence 산출

출력: JSON (10절 참조)
```

---

## 1. 기능(move) 태깅 기준

턴 번호와 무관하게, 각 사용자 발화가 수행한 기능을 태깅한다. 한 발화가 복수 기능을 수행할 수 있고, 기능이 생략될 수도 있다.

| 기능 | 태깅 조건 |
|---|---|
| M1 문제 설계 | 문제·상황·산출물·제약 중 2개 이상을 제시하며 작업의 틀을 잡음 |
| M2 방향 확정 | AI 답변 중 일부를 선택·배제하거나 범위·타깃을 조정함 |
| M3 초안 요청 | 구조(항목·형식)를 지정하며 중간 산출물을 요청함 |
| M4 검증 | 구체적 점검 기준을 제시하며 산출물 검토를 요청함 |
| M5 최종화 | 검토 결과를 반영해 사용 가능한 최종 형태를 요청함 |

순서 타당성 체크 (기능 커버리지와 별도):

- 정상 순서: M1 → (M2~M4 순서 무관, 단 M4는 검토 대상 산출물 존재 이후) → M5
- 위반 예: 초안 없이 검증 요청, 검증 없이 최종화 후 종료

---

## 2. 공통 채점 규칙

모든 축은 0~4점. 앵커는 4/2/0만 정의하고, 3·1점은 인접 앵커 사이로 판단한다.

| 점수 | 일반 기준 |
|---|---|
| 4 | 정의된 행동이 명확하고 구체적으로 수행됨 |
| 2 | 수행되었으나 불완전·부분적 |
| 0 | 수행되지 않았거나 형식적 수행에 그침 |

Judge 공통 제약 (기획안 §8 반영):

1. 축 1~7은 **사용자 발화만** 평가한다. AI 응답의 품질을 사용자 점수에 반영하지 않는다.
2. 프롬프트 길이는 가점 사유가 아니다. 길지만 비구조적인 발화는 짧고 명확한 발화보다 높게 채점하지 않는다.
3. 첫 턴에 모든 것을 요구하는 만능 요청은 M1만 인정하고, 작업 분해(축4)에서 감점한다.
4. 단순 동의("좋아, 계속해줘")는 어떤 기능으로도 태깅하지 않는다.
5. 시나리오 밖 해결책(주어진 맥락에서 실행 불가능한 제안 유도)은 축8에서 감점한다.
6. 각 점수에는 사용자 발화에서 직접 인용한 근거를 1개 이상 첨부한다.

---

## 3. 축별 루브릭

### 축 1. 목표 정의 (주 측정: M1)

| 점수 | 앵커 |
|---|---|
| 4 | 해결할 문제 + 기대 산출물을 모두 명시. AI가 추가 추론 없이 작업 목표를 특정 가능 |
| 2 | 문제 또는 산출물 중 하나만 명시, 혹은 둘 다 있으나 모호함 |
| 0 | "개선안 짜줘" 수준. 문제도 산출물도 특정 불가 |

- 4점 예: "전환율이 5%로 낮습니다. 퍼널 단계 분석 프레임과 단계별 가설·지표를 정리해주세요."
- 0점 예: "헬스케어 앱 전환율 개선안 짜줘."

### 축 2. 맥락 제공 (주 측정: M1, 이후 턴 보강 반영)

| 점수 | 앵커 |
|---|---|
| 4 | 배경·대상·제약·참고정보 중 3개 이상을 시나리오에 맞게 제공. AI가 일반론이 아닌 맞춤 판단 가능 |
| 2 | 1~2개 요소만 제공, 또는 제공했으나 판단에 불충분 |
| 0 | 맥락 없음. AI가 일반론으로만 답할 수밖에 없는 상태 |

- 감점: 시나리오에 이미 주어진 정보를 누락해 AI가 되묻게 만든 경우.
- 가점 아님: 시나리오와 무관한 배경의 장황한 나열.

### 축 3. 정보 구조화 (주 측정: M1, M3)

| 점수 | 앵커 |
|---|---|
| 4 | 지시·배경·자료·조건이 구분자/섹션/순서로 분리되어 혼동 여지 없음 |
| 2 | 부분적 구분. 일부 요소가 한 문단에 뒤섞임 |
| 0 | 모든 요소가 구분 없이 흘러가는 단일 문장·문단 |

### 축 4. 작업 분해 (주 측정: 전체 기능 흐름)

| 점수 | 앵커 |
|---|---|
| 4 | M1~M5가 단계적으로 수행되고, 각 턴 요청이 직전 산출물 위에 쌓임 |
| 2 | 단계 의도는 있으나 일부 턴이 중복되거나 비약함 |
| 0 | 첫 턴 만능 요청 후 잔여 턴을 소모하거나, 동일 요청 반복 |

- 0점 예: "상황 분석, KPI 설계, 전략 수립, 보고서 작성까지 한 번에 완성해줘."

### 축 5. 출력 설계 (주 측정: M3, M5)

| 점수 | 앵커 |
|---|---|
| 4 | 형식 + 포함 항목 + 용도(누가 어떻게 쓸지) 중 2개 이상 명시 |
| 2 | "표로 정리해줘" 수준의 형식만 지정 |
| 0 | 출력 형태에 대한 언급 없음 |

- 4점 예: "팀 회의에서 바로 공유할 표로, 원인 가설·확인 KPI·우선순위·다음 액션을 포함해주세요."

### 축 6. 상호작용 조율 (주 측정: M2, 2~5턴)

| 점수 | 앵커 |
|---|---|
| 4 | AI 답변의 구체 내용을 인용·선택하며 방향을 재설정. 판단의 근거가 드러남 |
| 2 | 방향 지시는 있으나 AI 답변과의 연결이 약함 (답변을 안 읽어도 가능한 지시) |
| 0 | "좋아, 계속해줘" 등 단순 동의·진행 요청만 존재 |

- 4점 예: "제안한 가설 중 결제 전 이탈에 집중하겠습니다. 타깃은 20대 직장인으로 좁혀주세요."
- 0점 예: "좋아. 계속해줘."

### 축 7. 검증 유도 (주 측정: M4)

| 점수 | 앵커 |
|---|---|
| 4 | 점검 기준 2개 이상을 명시한 검증 요청 (예: 논리 비약, 누락 KPI, 데이터 검증 가능성) |
| 2 | 검증 의도는 있으나 기준이 1개 이하이거나 추상적 |
| 0 | 검증 요청 없음, 또는 "검토해줘"·"더 좋게 해줘" 수준의 형식적 요청 |

- 4점 예: "초안에서 논리 비약, 빠진 KPI, 데이터로 확인 어려운 가설을 먼저 지적해주세요."
- 0점 예: "검토해줘."

### 축 8. 실무 적용 (측정 대상: 최종 결과물 — 사용자 발화와 분리 평가)

| 점수 | 앵커 |
|---|---|
| 4 | 시나리오 맥락 내에서 추가 가공 없이 즉시 사용 가능. 실행 주체·다음 액션이 특정됨 |
| 2 | 골격은 갖췄으나 실무 투입 전 보완 필요 |
| 0 | 일반론 수준이거나 시나리오 맥락에서 실행 불가능 |

- 주의: 이 축은 AI가 만든 결과물을 평가하지만, 점수의 의미는 "사용자가 5턴을 통해 결과물을 여기까지 끌어왔는가"이다.

---

## 4. 발전(델타) 점수

축별 점수와 별도로, 대화를 통한 개선 폭을 채점한다.

판단 방법: Judge는 (a) 1턴 발화만으로 AI가 냈을 기대 결과물 수준과 (b) 실제 최종 결과물 수준을 비교한다.

| 점수 | 앵커 |
|---|---|
| 4 | 2~5턴의 개입이 결과물의 구조·정확성·실무성을 명확히 향상시킴. 개입과 개선의 인과가 추적 가능 |
| 2 | 일부 개선이 있으나 특정 개입 덕분인지 불분명, 또는 개선이 표면적(형식만 변경) |
| 0 | 최종 결과물이 1턴 직후 수준과 사실상 동일 |

- 이 점수가 있어야 "5턴 내내 좋은 프롬프트를 썼지만 결과물은 정체"인 케이스와 "개입할 때마다 결과물이 좋아진" 케이스가 구분된다.

---

## 5. 종합 점수 산식 (권장 초안)

```text
종합 점수 (100점 만점)
├─ LLM Judge 원점수 80점
│  ├─ 프로세스 점수 50점: 축1~7 평균 × 12.5
│  ├─ 발전 점수 15점: 델타 점수 × 3.75
│  └─ 결과 점수 15점: 축8 × 3.75
└─ 최종 환산: LLM Judge 원점수 ÷ 80 × 100
```

- 코드 체크는 턴 완료, 형식 신호, 검증 표현 유무 등을 기록하지만 최종 점수에 직접 가산하지 않는다.
- 시나리오 재진술만으로 키워드 점수를 얻는 문제를 막기 위해 코드 체크는 Judge 보조·QA·신뢰도 판단에만 사용한다.
- 가중치는 파일럿 데이터로 보정 예정이며, 산식보다 축별 점수·근거가 리포트의 핵심이다.

---

## 6. 신뢰도(confidence) 산출

Judge는 평가와 별도로 신뢰도를 산출한다. 다음 조건마다 신뢰도를 낮춘다.

- 5턴 미완료 (중도 이탈)
- 사용자 발화 총량이 극히 적어 근거 인용 불가
- 시나리오 이탈로 평가 기준 적용이 어려움
- 기능 태깅 결과 M1이 부재 (작업 틀 자체가 없음)

신뢰도 low인 경우 리포트에 "평가 근거 부족" 문구를 표기하고 학습 미션 추천을 보수적으로 한다.

---

## 7. Judge 출력 형식

```json
{
  "move_tagging": [
    {"turn": 1, "moves": ["M1"], "note": "문제·산출물 제시"},
    {"turn": 2, "moves": ["M2"], "note": "가설 선택, 타깃 축소"}
  ],
  "sequence_valid": true,
  "axis_scores": [
    {
      "axis": "목표 정의",
      "score": 3,
      "evidence": "사용자 1턴: '전환율이 5%로 낮습니다...'",
      "comment": "문제는 명확하나 최종 산출물 형태 미지정"
    }
  ],
  "delta_score": {
    "score": 3,
    "evidence": "4턴 검증 요청 이후 KPI 누락 3건 보완됨",
    "t1_expected_level": "일반적 퍼널 분석 목록",
    "final_level": "우선순위·다음 액션 포함 실행안"
  },
  "best_intervention": {"turn": 4, "reason": "구체적 검증 기준 제시"},
  "missed_intervention": "2턴에서 타깃 정보 보강 기회를 놓침",
  "confidence": "high",
  "summary_strengths": "...",
  "summary_weaknesses": "..."
}
```

---

## 8. 미해결 항목 (다음 작업)

- 시나리오별 기준 고정값 (시나리오 설계 시 축별 4점 앵커를 시나리오 맞춤으로 구체화)
- 코드화 체크리스트의 진단 항목 및 Judge 교차 검증 규칙 구체화 → 3번 플로우 설계에서 다룸
- 파일럿 후 가중치·앵커 보정
