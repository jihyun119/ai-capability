# 푸키: AI시대에서 살아남기 백엔드 개발 스키마 정의

> AI 코딩 어시스턴트(Claude Code, Codex 등)에 컨텍스트로 제공하기 위한 문서입니다.
> 백엔드 개발 시 이 문서를 기준으로 작업하세요.

---

## 📁 문서 구조

```
docs/schema/
├── common.schema.md          ← 공통 원칙 + 공통 결과 테이블 (이 문서)
├── track1.schema.json        ← 트랙 1 DB 스키마 (세부)
├── track2.schema.json        ← 트랙 2 DB 스키마 (세부)
└── respondent_summary.sql    ← 공통 결과 요약 테이블
```

---

## 1. 공통 원칙

### 1-1. API 공통 응답 구조

모든 API 응답은 아래 공통 필드를 포함합니다.

**성공 응답**
```json
{
  "status": "success",
  "track": "track1",
  "resultId": "res_abc123",
  "version": "2026-05-24",
  "createdAt": "2026-05-24T12:00:00.000Z"
}
```

**에러 응답**
```json
{
  "status": "error",
  "track": "track1",
  "error": {
    "code": "INVALID_INPUT",
    "message": "입력값이 올바르지 않습니다.",
    "retryable": true
  }
}
```

---

### 1-2. DB 공통 그룹 구조

트랙 1, 2 테이블 모두 아래 그룹 구조를 동일하게 따릅니다.

| 그룹명 | 설명 |
|---|---|
| `respondent_info` | 응시자 기본 정보 (닉네임, 성별, 출생년도) |
| `mc_raw_answers` | 객관식 원본 응답 |
| `mc_scores` | 객관식 축별 환산 점수 (40% 반영 전) |
| `prompt_input` | 프롬프트 파트 입력 원본 (Track1: 외부 LLM 붙여넣기 결과 / Track2: 줄글) |
| `prompt_llm_evidence` | 프롬프트 파트에서 추출 또는 정리된 축별 판단 근거 |
| `prompt_scores` | 프롬프트 축별 점수 (60% 반영 전) |
| `final_scores` | 최종 통합 점수 (mc×0.4 + prompt×0.6) |
| `final_result` | 최종 결과 (유형 또는 등급) |
| `llm_raw_output` | LLM 원본 출력 전체 보관 |
| `created_at` | 응시 일시 |

---

### 1-3. DB 공통 메타 필드

모든 테이블에 포함되는 공통 필드입니다.

```json
{
  "result_id": "UUID (PK, gen_random_uuid())",
  "track": "track1 | track2",
  "version": "string (API 버전)",
  "status": "success | error",
  "created_at": "TIMESTAMP DEFAULT now()"
}
```

---

### 1-4. 채점 공통 공식

```
최종 축 점수 = mc_score × 0.4 + prompt_score × 0.6
```

- 객관식(mc) 40% + 프롬프트(prompt) 60% 가중 합산
- 트랙 1, 2 모두 동일한 공식 적용

---

## 2. Track 1 스키마

### 2-1. API Request

```json
{
  "questionnaireVersion": "track1-12",
  "questionnaire": {
    "answers": {
      "Q1": 5,
      "Q2": 4,
      "Q3": 5,
      "Q4": 2,
      "Q5": 2,
      "Q6": 2,
      "Q7": 3,
      "Q8": 3,
      "Q9": 4,
      "Q10": 5,
      "Q11": 5,
      "Q12": 4
    }
  },
  "llmResult": {
    "status": "success",
    "evidence_mode": "visible_history",
    "evidence_notice": "Assessment based on repeated observable interaction patterns.",
    "signals": {
      "A": "high",
      "B": "low",
      "C": "medium",
      "D": "high"
    },
    "confidence": {
      "A": "high",
      "B": "medium",
      "C": "medium",
      "D": "high"
    },
    "notes": {
      "A": "Frequently integrates AI into ongoing task workflows.",
      "B": "Uses a task-focused tone with limited emotional engagement.",
      "C": "Builds on useful AI outputs while checking details as quality control.",
      "D": "Consistently specifies goals, structure, format, and revision direction."
    },
    "verdict": "A structured user who treats AI as a productivity tool.",
    "tags": ["workflow-heavy", "task-focused", "directive"]
  }
}
```

---

### 2-2. API Response

```json
{
  "status": "success",
  "track": "track1",
  "version": "track1-v1",
  "resultId": "res_abc123",
  "createdAt": "2026-05-24T12:00:00.000Z",
  "decisionState": "diagnosable",
  "type": {
    "id": 12,
    "name": "선긋는 상사형"
  },
  "binaryProfile": {
    "A": "고",
    "B": "저",
    "C": "고",
    "D": "고"
  },
  "axisScores": {
    "A": {
      "label": "의존도",
      "score": 86,
      "level": "높음",
      "gauge": "■■■■■■■■■░"
    },
    "B": {
      "label": "친밀도",
      "score": 31,
      "level": "낮음",
      "gauge": "■■■░░░░░░░"
    },
    "C": {
      "label": "신뢰도",
      "score": 58,
      "level": "중간",
      "gauge": "■■■■■■░░░░"
    },
    "D": {
      "label": "통제욕구",
      "score": 91,
      "level": "높음",
      "gauge": "■■■■■■■■■░"
    }
  },
  "resultCard": {
    "title": "선긋는 상사형",
    "description": "자주 쓰지만 휘둘리진 않습니다.\n조건은 정확히 줍니다.\n결과는 끝까지 직접 판단합니다.\n\nAI를 믿기보다 부리는 타입.",
    "keywords": ["업무형", "거리두기", "명확한지시"],
    "reasonStory": [
      "AI를 그냥 맡기지 않습니다.",
      "조건을 세우고 방향을 잡습니다.",
      "답이 와도 그대로 두지 않습니다.",
      "끝까지 직접 판단합니다.",
      "그래서 이 유형은 믿기보다 부리는 사람에 가깝습니다."
    ],
    "evidenceNotice": "확인된 대화 기록 기반 결과입니다."
  }
}
```

---

### 2-3. DB Record Schema

```json
{
  "result_id": "UUID (PK)",
  "track": "track1",
  "version": "track1-v1",
  "status": "success",

  "respondent_info": {
    "nickname": "VARCHAR(50) NOT NULL",
    "gender": "VARCHAR(10) NULLABLE — male | female | other | null",
    "birth_year": "SMALLINT NULLABLE — ex) 2000"
  },

  "mc_raw_answers": {
    "q1_answer": "SMALLINT 1~5 / 의존도(A) / AI 없이도 OK ↔ AI 없으면 불편",
    "q2_answer": "SMALLINT 1~5 / 의존도(A) / 새 AI 서비스 굳이 안 써봄 ↔ 일단 써봄",
    "q3_answer": "SMALLINT 1~5 / 의존도(A) / AI 없던 시절 괜찮음 ↔ 상상불가",
    "q4_answer": "SMALLINT 1~5 / 친밀도(B) / 고민 안 털어놓음 ↔ 털어놓음",
    "q5_answer": "SMALLINT 1~5 / 친밀도(B) / AI는 그냥 기계 ↔ AI가 이해한다고 느낌",
    "q6_answer": "SMALLINT 1~5 / 친밀도(B) / 결과물이 중요 ↔ 대화 과정 즐거움",
    "q7_answer": "SMALLINT 1~5 / 신뢰도(C) / 항상 의심·검증 ↔ 대체로 신뢰",
    "q8_answer": "SMALLINT 1~5 / 신뢰도(C) / 교차 확인함 ↔ 검증 없이 씀",
    "q9_answer": "SMALLINT 1~5 / 신뢰도(C) / 내 판단 신뢰 ↔ AI 판단이 나을 때 많음",
    "q10_answer": "SMALLINT 1~5 / 통제욕구(D) / AI가 알아서 ↔ 내가 직접 방향 정함",
    "q11_answer": "SMALLINT 1~5 / 통제욕구(D) / 그냥 씀 ↔ 원하는 방향으로 수정 반복",
    "q12_answer": "SMALLINT 1~5 / 통제욕구(D) / 개입 안 함 ↔ 중간중간 점검·수정"
  },

  "mc_scores": {
    "mc_score_A": "SMALLINT — round(((Q1+Q2+Q3-3)/12)*100)",
    "mc_score_B": "SMALLINT — round(((Q4+Q5+Q6-3)/12)*100)",
    "mc_score_C": "SMALLINT — round(((Q7+Q8+Q9-3)/12)*100)",
    "mc_score_D": "SMALLINT — round(((Q10+Q11+Q12-3)/12)*100)"
  },

  "prompt_input": {
    "raw_pasted_llm_result": "TEXT NULLABLE — 유저가 외부 LLM에서 받은 원본 출력 전체",
    "parsed_llm_result": "JSONB NULLABLE — 파싱 및 검증된 외부 LLM 결과 JSON"
  },

  "prompt_llm_evidence": {
    "note_A": "TEXT NULLABLE — 외부 LLM의 의존도 판단 노트",
    "note_B": "TEXT NULLABLE — 외부 LLM의 친밀도 판단 노트",
    "note_C": "TEXT NULLABLE — 외부 LLM의 신뢰도 판단 노트",
    "note_D": "TEXT NULLABLE — 외부 LLM의 통제욕구 판단 노트"
  },

  "prompt_scores": {
    "prompt_score_A": "SMALLINT NULLABLE — signal=high:70~95 / medium:40~69 / low:10~39",
    "prompt_score_B": "SMALLINT NULLABLE",
    "prompt_score_C": "SMALLINT NULLABLE",
    "prompt_score_D": "SMALLINT NULLABLE"
  },

  "final_scores": {
    "score_A": "SMALLINT — mc_score_A×0.4 + prompt_score_A×0.6",
    "score_B": "SMALLINT — mc_score_B×0.4 + prompt_score_B×0.6",
    "score_C": "SMALLINT — mc_score_C×0.4 + prompt_score_C×0.6",
    "score_D": "SMALLINT — mc_score_D×0.4 + prompt_score_D×0.6",
    "level_A": "VARCHAR(6) — high | low (56↑=high / 44↓=low / 45~55=tie-zone)",
    "level_B": "VARCHAR(6) — high | low",
    "level_C": "VARCHAR(6) — high | low",
    "level_D": "VARCHAR(6) — high | low"
  },

  "tie_resolution": {
    "tie_axes": "VARCHAR(1)[] NULLABLE — 45~55 중간 범위에 해당한 축 목록 ex) ['C']",
    "source_A": "VARCHAR(30) NULLABLE — local_threshold | llm_judge | questionnaire_fallback | score_fallback",
    "source_B": "VARCHAR(30) NULLABLE — local_threshold | llm_judge | questionnaire_fallback | score_fallback",
    "source_C": "VARCHAR(30) NULLABLE — local_threshold | llm_judge | questionnaire_fallback | score_fallback",
    "source_D": "VARCHAR(30) NULLABLE — local_threshold | llm_judge | questionnaire_fallback | score_fallback",
    "reason_A": "TEXT NULLABLE — A축 고/저 판정 근거",
    "reason_B": "TEXT NULLABLE — B축 고/저 판정 근거",
    "reason_C": "TEXT NULLABLE — C축 고/저 판정 근거",
    "reason_D": "TEXT NULLABLE — D축 고/저 판정 근거"
  },

  "final_result": {
    "type_code": "SMALLINT 1~16 — level_A/B/C/D 조합으로 매핑",
    "type_name": "VARCHAR(50) — ex) 선긋는 상사형",
    "reason_story": "TEXT NULLABLE — 유형 분류 이유 설명"
  },

  "llm_raw_output": {
    "llm_raw_json": "JSONB NULLABLE — 외부 LLM 원본 JSON 전체",
    "llm_evidence_mode": "VARCHAR(30) — visible_history | memory_or_impression | self_report | minimal",
    "llm_signal_A": "VARCHAR(6) — low | medium | high",
    "llm_signal_B": "VARCHAR(6) — low | medium | high",
    "llm_signal_C": "VARCHAR(6) — low | medium | high",
    "llm_signal_D": "VARCHAR(6) — low | medium | high",
    "llm_confidence_A": "VARCHAR(6) — low | medium | high",
    "llm_confidence_B": "VARCHAR(6) — low | medium | high",
    "llm_confidence_C": "VARCHAR(6) — low | medium | high",
    "llm_confidence_D": "VARCHAR(6) — low | medium | high",
    "llm_verdict": "TEXT NULLABLE — 종합 평가 텍스트",
    "llm_tags": "VARCHAR(30)[] — exactly 3 tags, ex) ['analytical', 'directive', 'iterative']"
  },

  "created_at": "TIMESTAMP DEFAULT now()"
}
```

---

### 2-4. 트랙 1 유형 매핑 테이블

| type_code | type_name | A(의존도) | B(친밀도) | C(신뢰도) | D(통제욕구) |
|---|---|---|---|---|---|
| 1 | AI 몰라형 | low | low | low | low |
| 2 | 시키는만큼만 해 형 | low | low | low | high |
| 3 | 프로 검색러형 | low | low | high | low |
| 4 | 냉철한 조련사형 | low | low | high | high |
| 5 | 가벼운 수다쟁이형 | low | high | low | low |
| 6 | 의심많은 단골형 | low | high | low | high |
| 7 | 필찾하는 친구형 | low | high | high | low |
| 8 | 따뜻한 완벽주의자형 | low | high | high | high |
| 9 | 불안한 상습의뢰인형 | high | low | low | low |
| 10 | 프로 트집러형 | high | low | low | high |
| 11 | 드라이한 비즈니스맨형 | high | low | high | low |
| 12 | 선긋는 상사형 | high | low | high | high |
| 13 | 감정 쓰레기통형 | high | high | low | low |
| 14 | 애정 넘치는 경계형 | high | high | low | high |
| 15 | 든든한 파트너형 | high | high | high | low |
| 16 | 집착하는 애인형 | high | high | high | high |

---

### 2-5. 트랙 1 인덱스

```sql
CREATE INDEX idx_track1_type_code  ON track1_results(type_code);
CREATE INDEX idx_track1_birth_year ON track1_results(birth_year);
CREATE INDEX idx_track1_created_at ON track1_results(created_at);
```

---

## 3. Track 2 스키마

### 3-1. API Request

```json
{
  "answers": {
    "Q1": "D",
    "Q2": "D",
    "Q3": "B",
    "Q4": "C"
  },
  "freeText": "I usually give the AI my goal, audience, and constraints before asking for an output..."
}
```

> LLM feature extractor는 백엔드에서 호출합니다. 프론트는 위 Request만 전송하면 됩니다.

---

### 3-2. 백엔드 내부 Feature Schema

> 프론트에 노출되지 않습니다. 백엔드 내부에서만 사용합니다.

```json
{
  "task_clarity": {
    "evidence": "I usually give the AI my goal",
    "frequency": "frequently",
    "features": {
      "has_goal": true,
      "has_constraints": true,
      "has_scope": false
    }
  },
  "context": {
    "evidence": "audience",
    "frequency": "frequently",
    "features": {
      "has_purpose": true,
      "has_background": false,
      "has_audience": true
    }
  }
}
```

---

### 3-3. API Response

```json
{
  "status": "success",
  "track": "track2",
  "version": "track2-v1",
  "resultId": "res_xyz789",
  "total": 78.4,
  "grade": "AI 활용형",
  "usageType": "구조설계형",
  "axes": {
    "task_clarity": {
      "label": "작업 명확성",
      "score": 17.2,
      "max": 20,
      "rate": 0.86,
      "evidence": "I usually give the AI my goal"
    },
    "context": {
      "label": "배경·맥락",
      "score": 15.6,
      "max": 20,
      "rate": 0.78,
      "evidence": "audience"
    },
    "role": {
      "label": "역할 지정",
      "score": 8.5,
      "max": 15,
      "rate": 0.57,
      "evidence": "NONE"
    },
    "output_format": {
      "label": "출력 형식",
      "score": 11.2,
      "max": 15,
      "rate": 0.75,
      "evidence": "table format"
    },
    "iteration": {
      "label": "반복 개선",
      "score": 13.0,
      "max": 15,
      "rate": 0.87,
      "evidence": "I ask it to revise"
    },
    "critical_review": {
      "label": "비판적 검토",
      "score": 12.9,
      "max": 15,
      "rate": 0.86,
      "evidence": "I check whether the answer fits"
    }
  },
  "feedback": {
    "strengths": ["작업 명확성", "반복 개선"],
    "weaknesses": ["역할 지정", "배경·맥락"],
    "summary": "요청의 목표와 수정 방향을 비교적 잘 잡는 편입니다. 다만 AI에게 어떤 전문가 역할로 답해야 하는지 더 명확히 지정하면 결과의 일관성이 좋아집니다."
  }
}
```

---

### 3-4. DB Record Schema

```json
{
  "result_id": "UUID (PK)",
  "track": "track2",
  "version": "track2-v1",
  "status": "success",

  "respondent_info": {
    "nickname": "VARCHAR(50) NOT NULL",
    "gender": "VARCHAR(10) NULLABLE — male | female | other | null",
    "birth_year": "SMALLINT NULLABLE — ex) 2000"
  },

  "mc_raw_answers": {
    "q1_answer": "VARCHAR(1) A~E / 매출 데이터 오류 발견 시 대응 방식",
    "q2_answer": "VARCHAR(1) A~E / 반복 보고서 작업 효율화 전략",
    "q3_answer": "VARCHAR(1) A~E / 이탈률 개선 전략 수립 시 첫 프롬프트",
    "q4_answer": "VARCHAR(1) A~E / AI 답변 거부 시 우회 전략"
  },

  "mc_scores": {
    "mc_score_task_clarity": "SMALLINT max=20 / 기여문항: Q1 Q2 Q3 Q4",
    "mc_score_context":      "SMALLINT max=20 / 기여문항: Q1 Q3 Q4",
    "mc_score_role":         "SMALLINT max=15 / 기여문항: Q2 Q3 Q4",
    "mc_score_format":       "SMALLINT max=15 / 기여문항: Q1 Q2",
    "mc_score_iteration":    "SMALLINT max=15 / 기여문항: Q1 Q2 Q3",
    "mc_score_critical":     "SMALLINT max=15 / 기여문항: Q1 Q3 Q4"
  },

  "prompt_input": {
    "free_text": "TEXT NOT NULL — 유저 AI가 생성한 줄글 원본"
  },

  "prompt_llm_evidence": {
    "evidence_task_clarity": "TEXT NULLABLE — 작업 명확성 판단 근거 구절",
    "evidence_context":      "TEXT NULLABLE — 배경·맥락 판단 근거 구절",
    "evidence_role":         "TEXT NULLABLE — 역할 지정 판단 근거 구절",
    "evidence_format":       "TEXT NULLABLE — 출력 형식 판단 근거 구절",
    "evidence_iteration":    "TEXT NULLABLE — 반복 개선 판단 근거 구절",
    "evidence_critical":     "TEXT NULLABLE — 비판적 검토 판단 근거 구절"
  },

  "prompt_scores": {
    "prompt_score_task_clarity": "SMALLINT NULLABLE max=20 / 20:consistently 모두명시 / 16:usually / 10:sometimes / 4:rarely / 0:NONE",
    "prompt_score_context":      "SMALLINT NULLABLE max=20 / 20:consistently 목적+배경+대상 / 16:frequently / 10:sometimes / 4:rarely / 0:NONE",
    "prompt_score_role":         "SMALLINT NULLABLE max=15 / 15:frequently 구체적페르소나 / 12:sometimes / 8:occasionally / 3:rarely / 0:NONE",
    "prompt_score_format":       "SMALLINT NULLABLE max=15 / 15:frequently 형식+길이+톤 / 12:두가지 / 8:한가지 / 3:rarely / 0:NONE",
    "prompt_score_iteration":    "SMALLINT NULLABLE max=15 / 15:consistently 정확히짚고수정 / 12:usually / 8:sometimes모호 / 3:rarely / 0:NONE",
    "prompt_score_critical":     "SMALLINT NULLABLE max=15 / 15:consistently 직접반박 / 12:frequently / 8:occasionally / 3:rarely / 0:NONE"
  },

  "final_scores": {
    "score_task_clarity": "SMALLINT max=20 — mc×0.4 + prompt×0.6",
    "score_context":      "SMALLINT max=20 — mc×0.4 + prompt×0.6",
    "score_role":         "SMALLINT max=15 — mc×0.4 + prompt×0.6",
    "score_format":       "SMALLINT max=15 — mc×0.4 + prompt×0.6",
    "score_iteration":    "SMALLINT max=15 — mc×0.4 + prompt×0.6",
    "score_critical":     "SMALLINT max=15 — mc×0.4 + prompt×0.6",
    "total_score":        "SMALLINT max=100 — 6개 축 합산"
  },

  "final_result": {
    "grade": "VARCHAR(20) — AI 파트너형(85↑) | AI 활용형(70~84) | AI 탐색형(55~69) | AI 입문형(40~54) | AI 초보형(40↓)",
    "strength_1": "VARCHAR(30) NULLABLE — 강점 축 1",
    "strength_2": "VARCHAR(30) NULLABLE — 강점 축 2",
    "weakness_1": "VARCHAR(30) NULLABLE — 약점 축 1",
    "weakness_2": "VARCHAR(30) NULLABLE — 약점 축 2",
    "feedback_text": "TEXT NULLABLE — 백엔드 LLM 총평"
  },

  "llm_raw_output": {
    "llm_text_output": "TEXT NULLABLE — 백엔드 LLM 텍스트 출력 원본 전체"
  },

  "created_at": "TIMESTAMP DEFAULT now()"
}
```

---

### 3-5. 트랙 2 객관식 채점 기준

| 문항 | 주제 | 최상(D) | 우수(C) | 보통(B/E) | 미흡(E/B) | 최저(A) |
|---|---|---|---|---|---|---|
| Q1 | AI 오류 대응 | 도구분리형 | 로직검증형 | 티칭형 | 수작업형 | 재촉형 |
| Q2 | 반복 작업 효율화 | 시스템구축형 | 샘플활용형 | 가이드추각형 | 보조활용형 | 단순반복형 |
| Q3 | 전략 기획 접근 | 프로세스설계형 | 역할부여형 | 검토비판형 | 맥락제공형 | 결과중심형 |
| Q4 | AI 거부 우회 | 프레임전환형 | 상황극형 | 대상확장형 | 도구전환형 | 재요청형 |

---

### 3-6. 트랙 2 인덱스

```sql
CREATE INDEX idx_track2_grade       ON track2_results(grade);
CREATE INDEX idx_track2_total_score ON track2_results(total_score);
CREATE INDEX idx_track2_birth_year  ON track2_results(birth_year);
CREATE INDEX idx_track2_created_at  ON track2_results(created_at);
```

---

## 4. AI 코딩 어시스턴트를 위한 개발 가이드

> Claude Code 또는 Codex에 이 문서를 컨텍스트로 제공할 때 함께 전달하세요.

### 4-1. 백엔드 구현 시 주의사항

**채점 로직**
- 객관식 점수(mc)와 줄글 점수(prompt)를 반드시 분리해서 저장
- 최종 점수 = mc × 0.4 + prompt × 0.6 공식 적용
- 원본 입력값(raw_answers, free_text)은 반드시 저장

**LLM 연동**
- 트랙 1: 외부 LLM(유저 AI) → 결과 JSON을 프론트가 받아서 전송
- 트랙 2: 백엔드에서 LLM 직접 호출하여 줄글 채점
- 트랙 1 외부 LLM은 숫자 점수(`profile`)를 반환하지 않고 `signals`, `confidence`, `notes`, `verdict`, `tags`만 반환
- 트랙 1 최종 점수와 16유형 매핑은 백엔드 로컬 채점 로직이 확정
- 트랙 1에서 45~55 중간 범위 축이 발생하면 LLM Judge가 notes 기반으로 보조 판정할 수 있음
- LLM 원본 출력은 항상 별도 컬럼에 보관 (재채점 대비)

**에러 처리**
- LLM JSON 파싱 실패 시 → `status: error` 반환, DB 저장 안 함
- 트랙 1에서 `evidence_mode: minimal` 이면 결과 신뢰도 낮음 표시

### 4-2. 추천 기술 스택

| 역할 | 추천 |
|---|---|
| 백엔드 프레임워크 | FastAPI (Python) |
| DB | PostgreSQL |
| ORM | SQLAlchemy |
| LLM 연동 | Anthropic API / OpenAI API |
| 배포 | Railway 또는 Render |

### 4-3. API 엔드포인트 구조 (예시)

```
POST /api/track1/submit     → 트랙 1 결과 제출 및 채점
POST /api/track2/submit     → 트랙 2 결과 제출 및 채점
GET  /api/track1/:resultId  → 트랙 1 결과 조회
GET  /api/track2/:resultId  → 트랙 2 결과 조회
```

---

## 5. 공통 결과 테이블 (respondent_summary)

> 트랙 1, 2 세부 테이블의 최종 결과만 뽑아서 한눈에 관리하는 요약 테이블입니다.
> 분석·집계·대시보드용으로 활용합니다.
> 세부 데이터가 필요하면 `track1_result_id`, `track2_result_id`로 각 트랙 테이블을 JOIN하세요.

---

### 5-1. 테이블 정의

```sql
CREATE TABLE respondent_summary (
    summary_id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),

    -- 응시자 기본 정보
    nickname            VARCHAR(50)     NOT NULL,
    gender              VARCHAR(10),                -- male | female | other | null
    birth_year          SMALLINT,                   -- ex) 2000

    -- 트랙 1 최종 결과
    track1_result_id    UUID,                       -- track1_results.result_id 참조 (FK)
    track1_type_code    SMALLINT,                   -- 1~16
    track1_type_name    VARCHAR(50),                -- ex) 선긋는 상사형
    track1_score_A      SMALLINT,                   -- 의존도 최종 점수
    track1_score_B      SMALLINT,                   -- 친밀도 최종 점수
    track1_score_C      SMALLINT,                   -- 신뢰도 최종 점수
    track1_score_D      SMALLINT,                   -- 통제욕구 최종 점수
    track1_completed_at TIMESTAMP,                  -- 트랙 1 완료 일시

    -- 트랙 2 최종 결과
    track2_result_id    UUID,                       -- track2_results.result_id 참조 (FK)
    track2_total_score  SMALLINT,                   -- 총점 (0~100)
    track2_grade        VARCHAR(20),                -- AI 파트너형 | AI 활용형 | AI 탐색형 | AI 입문형 | AI 초보형
    track2_completed_at TIMESTAMP,                  -- 트랙 2 완료 일시

    -- 트랙 3 최종 결과 (추후 추가 예정)
    track3_result_id    UUID,                       -- track3_results.result_id 참조 (FK, 추후)
    track3_total_score  SMALLINT,                   -- 총점 (추후)
    track3_grade        VARCHAR(20),                -- 등급 (추후)
    track3_completed_at TIMESTAMP,                  -- 트랙 3 완료 일시 (추후)

    created_at          TIMESTAMP NOT NULL DEFAULT now()
);
```

---

### 5-2. JSON Schema

```json
{
  "table": "respondent_summary",
  "description": "응시자별 트랙 최종 결과 요약 테이블",
  "primary_key": "summary_id",
  "columns": {

    "summary_id": {
      "type": "UUID",
      "nullable": false,
      "default": "gen_random_uuid()",
      "description": "요약 레코드 고유 ID"
    },

    "respondent_info": {
      "nickname": {
        "type": "VARCHAR(50)",
        "nullable": false,
        "description": "응시자 닉네임"
      },
      "gender": {
        "type": "VARCHAR(10)",
        "nullable": true,
        "enum": ["male", "female", "other", null],
        "description": "응시자 성별"
      },
      "birth_year": {
        "type": "SMALLINT",
        "nullable": true,
        "description": "응시자 출생년도 ex) 2000"
      }
    },

    "track1_result": {
      "track1_result_id": {
        "type": "UUID",
        "nullable": true,
        "reference": "track1_results.result_id",
        "description": "트랙 1 세부 결과 참조 ID"
      },
      "track1_type_code": {
        "type": "SMALLINT",
        "nullable": true,
        "range": "1~16",
        "description": "트랙 1 유형 코드"
      },
      "track1_type_name": {
        "type": "VARCHAR(50)",
        "nullable": true,
        "description": "트랙 1 유형명 ex) 선긋는 상사형"
      },
      "track1_score_A": {
        "type": "SMALLINT",
        "nullable": true,
        "description": "의존도 최종 점수 (0~100)"
      },
      "track1_score_B": {
        "type": "SMALLINT",
        "nullable": true,
        "description": "친밀도 최종 점수 (0~100)"
      },
      "track1_score_C": {
        "type": "SMALLINT",
        "nullable": true,
        "description": "신뢰도 최종 점수 (0~100)"
      },
      "track1_score_D": {
        "type": "SMALLINT",
        "nullable": true,
        "description": "통제욕구 최종 점수 (0~100)"
      },
      "track1_completed_at": {
        "type": "TIMESTAMP",
        "nullable": true,
        "description": "트랙 1 완료 일시"
      }
    },

    "track2_result": {
      "track2_result_id": {
        "type": "UUID",
        "nullable": true,
        "reference": "track2_results.result_id",
        "description": "트랙 2 세부 결과 참조 ID"
      },
      "track2_total_score": {
        "type": "SMALLINT",
        "nullable": true,
        "max_score": 100,
        "description": "트랙 2 총점"
      },
      "track2_grade": {
        "type": "VARCHAR(20)",
        "nullable": true,
        "enum": ["AI 파트너형", "AI 활용형", "AI 탐색형", "AI 입문형", "AI 초보형"],
        "description": "트랙 2 등급"
      },
      "track2_completed_at": {
        "type": "TIMESTAMP",
        "nullable": true,
        "description": "트랙 2 완료 일시"
      }
    },

    "track3_result": {
      "_comment": "추후 트랙 3 추가 시 채워집니다",
      "track3_result_id": {
        "type": "UUID",
        "nullable": true,
        "reference": "track3_results.result_id",
        "description": "트랙 3 세부 결과 참조 ID (추후)"
      },
      "track3_total_score": {
        "type": "SMALLINT",
        "nullable": true,
        "description": "트랙 3 총점 (추후)"
      },
      "track3_grade": {
        "type": "VARCHAR(20)",
        "nullable": true,
        "description": "트랙 3 등급 (추후)"
      },
      "track3_completed_at": {
        "type": "TIMESTAMP",
        "nullable": true,
        "description": "트랙 3 완료 일시 (추후)"
      }
    },

    "created_at": {
      "type": "TIMESTAMP",
      "nullable": false,
      "default": "now()",
      "description": "레코드 생성 일시"
    }
  }
}
```

---

### 5-3. 인덱스

```sql
CREATE INDEX idx_summary_track1_type   ON respondent_summary(track1_type_code);
CREATE INDEX idx_summary_track2_grade  ON respondent_summary(track2_grade);
CREATE INDEX idx_summary_birth_year    ON respondent_summary(birth_year);
CREATE INDEX idx_summary_created_at    ON respondent_summary(created_at);
```

---

### 5-4. 전체 테이블 관계도

```
respondent_summary (요약 테이블)
├── track1_result_id ──→ track1_results (세부 테이블)
├── track2_result_id ──→ track2_results (세부 테이블)
└── track3_result_id ──→ track3_results (추후 추가)
```

**저장 흐름**
```
유저가 트랙 1 완료
        ↓
track1_results 에 세부 결과 저장 → result_id 반환
        ↓
respondent_summary 에 요약 결과 upsert
(nickname + birth_year 기준으로 기존 레코드 있으면 update, 없으면 insert)

유저가 트랙 2 완료
        ↓
track2_results 에 세부 결과 저장 → result_id 반환
        ↓
respondent_summary 의 track2 필드 update
```

> ⚠️ 트랙 간 연결은 nickname + birth_year 조합으로 느슨하게 연결합니다.
> 동명이인 문제가 있을 수 있으나 MVP 수준에서는 허용합니다.
> 정확한 연결이 필요하면 추후 고유 코드 발급 방식으로 전환하세요.

---

### 5-5. 활용 예시 쿼리

**트랙 1, 2 모두 완료한 응시자 집계**
```sql
SELECT COUNT(*)
FROM respondent_summary
WHERE track1_type_name IS NOT NULL
  AND track2_grade IS NOT NULL;
```

**트랙 1 유형별 분포**
```sql
SELECT track1_type_name, COUNT(*) as count
FROM respondent_summary
WHERE track1_type_name IS NOT NULL
GROUP BY track1_type_name
ORDER BY count DESC;
```

**트랙 2 등급별 평균 출생년도**
```sql
SELECT track2_grade, AVG(birth_year) as avg_birth_year
FROM respondent_summary
WHERE track2_grade IS NOT NULL
GROUP BY track2_grade;
```

**컨퍼런스 발표용 — 전체 현황**
```sql
SELECT
  COUNT(*) AS total_respondents,
  COUNT(track1_type_name) AS track1_count,
  COUNT(track2_grade) AS track2_count,
  AVG(track2_total_score) AS avg_track2_score
FROM respondent_summary;
```

---

*마지막 업데이트: 2026년 5월 24일*
*BOAZ ADV 서비스 개발팀 — 푸키: AI시대에서 살아남기*
