# (수정) 공통 스키마 정의

상태: 진행 중

아래는 1차 문서 구조를 유지하면서 수정사항 1~13번을 반영한 최종 텍스트입니다.

```markdown
# 푸키-에.살.남 백엔드 개발 스키마 정의

> AI 코딩 어시스턴트(Claude Code, Codex 등)에 컨텍스트로 제공하기 위한 문서입니다.
백엔드 개발 시 이 문서를 기준으로 작업하세요.
>

---

## 📁 문서 구조
```

docs/schema/

├── common.schema.md

├── track1.schema.md

├── track2.schema.md

└── db.schema.md

```

---

## 1. 공통 원칙

### 1-1. API 공통 응답 구조

모든 API 응답은 아래 공통 필드를 포함합니다.

**성공 응답**

```json
{
  "status": "success",
  "track": "track1",
  "version": "track1-v1",
  "resultId": "res_abc123",
  "shareSlug": "abc123",
  "createdAt": "2026-05-24T12:00:00.000Z",
  "result": {}
}
```

**에러 응답**

```json
{
  "status": "error",
  "track": "track1",
  "version": "track1-v1",
  "error": {
    "code": "INVALID_INPUT",
    "message": "입력값이 올바르지 않습니다.",
    "retryable": true
  }
}
```

에러 코드는 MVP에서는 이 정도면 충분합니다.

```
INVALID_INPUT
INVALID_QUESTIONNAIRE
INVALID_LLM_RESULT
INSUFFICIENT_HISTORY
PRIVACY_BLOCKED
LLM_FEATURE_EXTRACTION_FAILED
INTERNAL_ERROR
```

---

### 1-2. DB 공통 그룹 구조

MVP DB는 아래 4개 테이블 기준으로 설계합니다.

```
1. respondents
2. track1_results
3. track2_results
4. diagnosis_answers
```

트랙 1, 2 테이블 모두 아래 그룹 구조를 기준으로 설계합니다.

| 그룹명 | 설명 |
| --- | --- |
| `respondents` | 로그인 없는 닉네임 기반 임시 응시자 정보 |
| `mc_raw_answers` | 객관식 원본 응답 |
| `mc_scores` | 객관식 축별 환산 점수 |
| `llm_result_sanitized` | Track 1 외부 LLM 결과 중 개인정보 제거/정제된 JSON |
| `extracted_features` | Track 2 백엔드 LLM이 freeText에서 추출한 evidence/frequency/features |
| `prompt_scores` | 프롬프트 기반 축별 점수 |
| `final_scores` | 최종 통합 점수 |
| `result_card` | 프론트가 결과 카드 렌더링할 JSON |
| `feedback` | Track 2 강점, 약점, 총평 JSON |
| `share_slug` | 공유 링크용 문자열 |
| `created_at` | 응시 일시 |

Track 2 freeText 원문과 LLM raw output 전체는 저장하지 않습니다.

개인정보 위험이 있으므로 extracted_features, prompt_scores, feedback만 저장합니다.

---

### 1-3. DB 공통 메타 필드

모든 결과 테이블에 포함되는 공통 필드입니다.

```json
{
  "result_id": "UUID (PK, gen_random_uuid())",
  "respondent_id": "UUID (FK, respondents.id)",
  "nickname_snapshot": "TEXT NOT NULL",
  "version": "string (API 버전)",
  "status": "success | error",
  "questionnaire_version": "string (문항 버전)",
  "share_slug": "TEXT UNIQUE",
  "created_at": "TIMESTAMPTZ DEFAULT now()"
}
```

---

### 1-4. 채점 공통 공식

```
최종 축 점수 = mc_score × 0.4 + prompt_score × 0.6
```

- 객관식(mc) 40% + 프롬프트(prompt) 60% 가중 합산
- 트랙 1, 2 모두 동일한 공식 적용

---

## 2. Track 1 스키마

### 2-1. API Request

```json
{
  "respondentId": "uuid",
  "accessToken": "token",
  "questionnaireVersion": "track1-12-v1",
  "questionnaire": {
    "answers": {
      "Q1": 5,
      "Q2": 4,
      "Q3": 5,
      "Q4": 2,
      "Q5": 2,
      "Q6": 2,
      "Q7": 3,
      "Q8": 3,
      "Q9": 4,
      "Q10": 5,
      "Q11": 5,
      "Q12": 4
    }
  },
  "llmResult": {
    "status": "success",
    "evidence_mode": "visible_history",
    "evidence_notice": "Assessment based on repeated observable interaction patterns.",
    "signals": {
      "A": "high",
      "B": "low",
      "C": "medium",
      "D": "high"
    },
    "confidence": {
      "A": "high",
      "B": "medium",
      "C": "medium",
      "D": "high"
    },
    "notes": {
      "A": "Frequently integrates AI into ongoing task workflows.",
      "B": "Uses a task-focused tone with limited emotional engagement.",
      "C": "Builds on useful AI outputs while checking details as quality control.",
      "D": "Consistently specifies goals, structure, format, and revision direction."
    },
    "verdict": "A structured user who treats AI as a productivity tool.",
    "tags": ["workflow-heavy", "task-focused", "directive"]
  }
}
```

---

### 2-2. API Response

```json
{
  "status": "success",
  "track": "track1",
  "version": "track1-v1",
  "resultId": "uuid",
  "shareSlug": "abc123",
  "createdAt": "2026-05-24T12:00:00.000Z",
  "result": {
    "decisionState": "diagnosable",
    "type": {
      "id": 12,
      "name": "선긋는 상사형"
    },
    "binaryProfile": {
      "A": "고",
      "B": "저",
      "C": "고",
      "D": "고"
    },
    "axisScores": {
      "A": {
        "label": "의존도",
        "score": 86,
        "level": "높음",
        "gauge": "■■■■■■■■■░"
      },
      "B": {
        "label": "친밀도",
        "score": 31,
        "level": "낮음",
        "gauge": "■■■░░░░░░░"
      },
      "C": {
        "label": "신뢰도",
        "score": 58,
        "level": "중간",
        "gauge": "■■■■■■░░░░"
      },
      "D": {
        "label": "통제욕구",
        "score": 91,
        "level": "높음",
        "gauge": "■■■■■■■■■░"
      }
    },
    "resultCard": {
      "title": "선긋는 상사형",
      "description": "자주 쓰지만 휘둘리진 않습니다.\\n조건은 정확히 줍니다.\\n결과는 끝까지 직접 판단합니다.\\n\\nAI를 믿기보다 부리는 타입.",
      "keywords": ["업무형", "거리두기", "명확한지시"],
      "reasonStory": [
        "AI를 그냥 맡기지 않습니다.",
        "조건을 세우고 방향을 잡습니다.",
        "답이 와도 그대로 두지 않습니다.",
        "끝까지 직접 판단합니다.",
        "그래서 이 유형은 믿기보다 부리는 사람에 가깝습니다."
      ],
      "evidenceNotice": "확인된 대화 기록 기반 결과입니다."
    }
  }
}
```

---

### 2-3. DB Record Schema

Track 1 결과는 `track1_results` 테이블에 저장합니다.

`respondent_info`를 직접 저장하지 않고 `respondent_id`로 `respondents`를 참조합니다.

결과 생성 당시 닉네임은 `nickname_snapshot`에 저장합니다.

```json
{
  "result_id": "UUID (PK)",
  "respondent_id": "UUID NOT NULL — respondents.id 참조",
  "nickname_snapshot": "TEXT NOT NULL — 결과 생성 당시 닉네임",

  "version": "track1-v1",
  "status": "success",
  "questionnaire_version": "track1-12-v1",

  "mc_raw_answers": {
    "Q1": "SMALLINT 1~5 / 의존도(A) / AI 없이도 OK ↔ AI 없으면 불편",
    "Q2": "SMALLINT 1~5 / 의존도(A) / 새 AI 서비스 굳이 안 써봄 ↔ 일단 써봄",
    "Q3": "SMALLINT 1~5 / 의존도(A) / AI 없던 시절 괜찮음 ↔ 상상불가",
    "Q4": "SMALLINT 1~5 / 친밀도(B) / 고민 안 털어놓음 ↔ 털어놓음",
    "Q5": "SMALLINT 1~5 / 친밀도(B) / AI는 그냥 기계 ↔ AI가 이해한다고 느낌",
    "Q6": "SMALLINT 1~5 / 친밀도(B) / 결과물이 중요 ↔ 대화 과정 즐거움",
    "Q7": "SMALLINT 1~5 / 신뢰도(C) / 항상 의심·검증 ↔ 대체로 신뢰",
    "Q8": "SMALLINT 1~5 / 신뢰도(C) / 교차 확인함 ↔ 검증 없이 씀",
    "Q9": "SMALLINT 1~5 / 신뢰도(C) / 내 판단 신뢰 ↔ AI 판단이 나을 때 많음",
    "Q10": "SMALLINT 1~5 / 통제욕구(D) / AI가 알아서 ↔ 내가 직접 방향 정함",
    "Q11": "SMALLINT 1~5 / 통제욕구(D) / 그냥 씀 ↔ 원하는 방향으로 수정 반복",
    "Q12": "SMALLINT 1~5 / 통제욕구(D) / 개입 안 함 ↔ 중간중간 점검·수정"
  },

  "mc_scores": {
    "A": "SMALLINT — round(((Q1+Q2+Q3-3)/12)*100)",
    "B": "SMALLINT — round(((Q4+Q5+Q6-3)/12)*100)",
    "C": "SMALLINT — round(((Q7+Q8+Q9-3)/12)*100)",
    "D": "SMALLINT — round(((Q10+Q11+Q12-3)/12)*100)"
  },

  "llm_result_sanitized": {
    "status": "success",
    "evidence_mode": "visible_history | memory_or_impression | self_report | minimal",
    "evidence_notice": "TEXT NULLABLE",
    "signals": {
      "A": "low | medium | high",
      "B": "low | medium | high",
      "C": "low | medium | high",
      "D": "low | medium | high"
    },
    "confidence": {
      "A": "low | medium | high",
      "B": "low | medium | high",
      "C": "low | medium | high",
      "D": "low | medium | high"
    },
    "notes": {
      "A": "TEXT NULLABLE",
      "B": "TEXT NULLABLE",
      "C": "TEXT NULLABLE",
      "D": "TEXT NULLABLE"
    },
    "verdict": "TEXT NULLABLE",
    "tags": "TEXT[]"
  },

  "prompt_scores": {
    "A": "SMALLINT NULLABLE — signal=high:70~95 / medium:40~69 / low:10~39",
    "B": "SMALLINT NULLABLE",
    "C": "SMALLINT NULLABLE",
    "D": "SMALLINT NULLABLE"
  },

  "final_scores": {
    "A": "SMALLINT — mc_score_A×0.4 + prompt_score_A×0.6",
    "B": "SMALLINT — mc_score_B×0.4 + prompt_score_B×0.6",
    "C": "SMALLINT — mc_score_C×0.4 + prompt_score_C×0.6",
    "D": "SMALLINT — mc_score_D×0.4 + prompt_score_D×0.6"
  },

  "type_code": "SMALLINT 1~16 — level_A/B/C/D 조합으로 매핑",
  "type_name": "TEXT — ex) 선긋는 상사형",

  "binary_profile": {
    "A": "고 | 저",
    "B": "고 | 저",
    "C": "고 | 저",
    "D": "고 | 저"
  },

  "result_card": {
    "title": "TEXT",
    "description": "TEXT",
    "keywords": "TEXT[]",
    "reasonStory": "TEXT[]",
    "evidenceNotice": "TEXT"
  },

  "evidence_mode": "TEXT NULLABLE — visible_history | memory_or_impression | self_report | minimal",
  "evidence_notice": "TEXT NULLABLE",

  "share_slug": "TEXT NOT NULL UNIQUE",

  "created_at": "TIMESTAMPTZ DEFAULT now()"
}
```

---

### 2-4. 트랙 1 유형 매핑 테이블

| type_code | type_name | A(의존도) | B(친밀도) | C(신뢰도) | D(통제욕구) |
| --- | --- | --- | --- | --- | --- |
| 1 | 도구형 | low | low | low | low |
| 2 | 단순 지시형 | low | low | low | high |
| 3 | 검증가형 | low | low | high | low |
| 4 | 전문가형 | low | low | high | high |
| 5 | 단순 교류형 | low | high | low | low |
| 6 | 감시자형 | low | high | low | high |
| 7 | 낙천적 방관형 | low | high | high | low |
| 8 | 애정 어린 감독관 | low | high | high | high |
| 9 | 비서형 | high | low | low | low |
| 10 | 독재자형 | high | low | low | high |
| 11 | 실무 신뢰형 | high | low | high | low |
| 12 | 권위적 관리자 | high | low | high | high |
| 13 | 감정형 | high | high | low | low |
| 14 | 경계형 | high | high | low | high |
| 15 | 파트너형 | high | high | high | low |
| 16 | 소유형 | high | high | high | high |

---

### 2-5. 트랙 1 인덱스

```sql
CREATE INDEX idx_track1_type_code  ON track1_results(type_code);
CREATE INDEX idx_track1_created_at ON track1_results(created_at);
```

---

## 3. Track 2 스키마

### 3-1. API Request

```json
{
  "respondentId": "uuid",
  "accessToken": "token",
  "questionnaireVersion": "track2-4-v1",
  "answers": {
    "Q1": "D",
    "Q2": "D",
    "Q3": "B",
    "Q4": "C"
  },
  "freeText": "I usually give the AI my goal, audience, and constraints before asking for an output..."
}
```

> LLM feature extractor는 백엔드에서 호출합니다. 프론트는 위 Request만 전송하면 됩니다.
> 

---

### 3-2. 백엔드 내부 Feature Schema

> 프론트에 노출되지 않습니다. 백엔드 내부에서만 사용합니다.
> 

```json
{
  "task_clarity": {
    "evidence": "I usually give the AI my goal",
    "frequency": "frequently",
    "features": {
      "has_goal": true,
      "has_constraints": true,
      "has_scope": false
    }
  },
  "context": {
    "evidence": "audience",
    "frequency": "frequently",
    "features": {
      "has_purpose": true,
      "has_background": false,
      "has_audience": true
    }
  }
}
```

---

### 3-3. API Response

```json
{
  "status": "success",
  "track": "track2",
  "version": "track2-v1",
  "resultId": "uuid",
  "shareSlug": "xyz789",
  "createdAt": "2026-05-24T12:00:00.000Z",
  "result": {
    "total": 78.4,
    "grade": "AI 활용형",
    "usageType": "구조설계형",
    "axes": {
      "task_clarity": {
        "label": "작업 명확성",
        "score": 17.2,
        "max": 20,
        "rate": 0.86,
        "evidence": "I usually give the AI my goal"
      },
      "context": {
        "label": "배경·맥락",
        "score": 15.6,
        "max": 20,
        "rate": 0.78,
        "evidence": "audience"
      },
      "role": {
        "label": "역할 지정",
        "score": 8.5,
        "max": 15,
        "rate": 0.57,
        "evidence": "NONE"
      },
      "output_format": {
        "label": "출력 형식",
        "score": 11.2,
        "max": 15,
        "rate": 0.75,
        "evidence": "table format"
      },
      "iteration": {
        "label": "반복 개선",
        "score": 13,
        "max": 15,
        "rate": 0.87,
        "evidence": "I ask it to revise"
      },
      "critical_review": {
        "label": "비판적 검토",
        "score": 12.9,
        "max": 15,
        "rate": 0.86,
        "evidence": "I check whether the answer fits"
      }
    },
    "feedback": {
      "strengths": ["작업 명확성", "반복 개선"],
      "weaknesses": ["역할 지정", "배경·맥락"],
      "summary": "요청의 목표와 수정 방향을 비교적 잘 잡는 편입니다. 다만 AI에게 어떤 전문가 역할로 답해야 하는지 더 명확히 지정하면 결과의 일관성이 좋아집니다."
    }
  }
}
```

---

### 3-4. DB Record Schema

Track 2 결과는 `track2_results` 테이블에 저장합니다.

`respondent_info`를 직접 저장하지 않고 `respondent_id`로 `respondents`를 참조합니다.

Track 2 freeText 원문은 저장하지 않습니다.

백엔드 LLM이 freeText에서 추출한 `extracted_features`, `prompt_scores`, `feedback`만 저장합니다.

```json
{
  "result_id": "UUID (PK)",
  "respondent_id": "UUID NOT NULL — respondents.id 참조",
  "nickname_snapshot": "TEXT NOT NULL — 결과 생성 당시 닉네임",

  "version": "track2-v1",
  "status": "success",
  "questionnaire_version": "track2-4-v1",

  "mc_raw_answers": {
    "Q1": "VARCHAR(1) A~E / 매출 데이터 오류 발견 시 대응 방식",
    "Q2": "VARCHAR(1) A~E / 반복 보고서 작업 효율화 전략",
    "Q3": "VARCHAR(1) A~E / 이탈률 개선 전략 수립 시 첫 프롬프트",
    "Q4": "VARCHAR(1) A~E / AI 답변 거부 시 우회 전략"
  },

  "mc_scores": {
    "task_clarity": "SMALLINT max=20 / 기여문항: Q1 Q2 Q3 Q4",
    "context": "SMALLINT max=20 / 기여문항: Q1 Q3 Q4",
    "role": "SMALLINT max=15 / 기여문항: Q2 Q3 Q4",
    "output_format": "SMALLINT max=15 / 기여문항: Q1 Q2",
    "iteration": "SMALLINT max=15 / 기여문항: Q1 Q2 Q3",
    "critical_review": "SMALLINT max=15 / 기여문항: Q1 Q3 Q4"
  },

  "extracted_features": {
    "task_clarity": {
      "evidence": "TEXT NULLABLE — 작업 명확성 판단 근거 구절",
      "frequency": "consistently | frequently | sometimes | rarely | none",
      "features": {
        "has_goal": "BOOLEAN",
        "has_constraints": "BOOLEAN",
        "has_scope": "BOOLEAN"
      }
    },
    "context": {
      "evidence": "TEXT NULLABLE — 배경·맥락 판단 근거 구절",
      "frequency": "consistently | frequently | sometimes | rarely | none",
      "features": {
        "has_purpose": "BOOLEAN",
        "has_background": "BOOLEAN",
        "has_audience": "BOOLEAN"
      }
    },
    "role": {
      "evidence": "TEXT NULLABLE — 역할 지정 판단 근거 구절",
      "frequency": "consistently | frequently | sometimes | rarely | none",
      "features": {
        "has_persona": "BOOLEAN",
        "has_expertise": "BOOLEAN"
      }
    },
    "output_format": {
      "evidence": "TEXT NULLABLE — 출력 형식 판단 근거 구절",
      "frequency": "consistently | frequently | sometimes | rarely | none",
      "features": {
        "has_format": "BOOLEAN",
        "has_length": "BOOLEAN",
        "has_tone": "BOOLEAN"
      }
    },
    "iteration": {
      "evidence": "TEXT NULLABLE — 반복 개선 판단 근거 구절",
      "frequency": "consistently | frequently | sometimes | rarely | none",
      "features": {
        "has_revision": "BOOLEAN",
        "has_specific_feedback": "BOOLEAN"
      }
    },
    "critical_review": {
      "evidence": "TEXT NULLABLE — 비판적 검토 판단 근거 구절",
      "frequency": "consistently | frequently | sometimes | rarely | none",
      "features": {
        "has_verification": "BOOLEAN",
        "has_counterargument": "BOOLEAN"
      }
    }
  },

  "prompt_scores": {
    "task_clarity": "SMALLINT NULLABLE max=20 / 20:consistently 모두명시 / 16:usually / 10:sometimes / 4:rarely / 0:NONE",
    "context": "SMALLINT NULLABLE max=20 / 20:consistently 목적+배경+대상 / 16:frequently / 10:sometimes / 4:rarely / 0:NONE",
    "role": "SMALLINT NULLABLE max=15 / 15:frequently 구체적페르소나 / 12:sometimes / 8:occasionally / 3:rarely / 0:NONE",
    "output_format": "SMALLINT NULLABLE max=15 / 15:frequently 형식+길이+톤 / 12:두가지 / 8:한가지 / 3:rarely / 0:NONE",
    "iteration": "SMALLINT NULLABLE max=15 / 15:consistently 정확히짚고수정 / 12:usually / 8:sometimes모호 / 3:rarely / 0:NONE",
    "critical_review": "SMALLINT NULLABLE max=15 / 15:consistently 직접반박 / 12:frequently / 8:occasionally / 3:rarely / 0:NONE"
  },

  "final_scores": {
    "task_clarity": "SMALLINT max=20 — mc×0.4 + prompt×0.6",
    "context": "SMALLINT max=20 — mc×0.4 + prompt×0.6",
    "role": "SMALLINT max=15 — mc×0.4 + prompt×0.6",
    "output_format": "SMALLINT max=15 — mc×0.4 + prompt×0.6",
    "iteration": "SMALLINT max=15 — mc×0.4 + prompt×0.6",
    "critical_review": "SMALLINT max=15 — mc×0.4 + prompt×0.6"
  },

  "total_score": "NUMERIC(5,2) max=100 — 6개 축 합산",
  "grade": "TEXT — AI 파트너형(85↑) | AI 활용형(70~84) | AI 탐색형(55~69) | AI 입문형(40~54) | AI 초보형(40↓)",
  "usage_type": "TEXT NULLABLE — 구조설계형, 반복개선형 등",

  "feedback": {
    "strengths": "TEXT[] — 강점 축",
    "weaknesses": "TEXT[] — 약점 축",
    "summary": "TEXT — 종합 피드백"
  },

  "share_slug": "TEXT NOT NULL UNIQUE",

  "created_at": "TIMESTAMPTZ DEFAULT now()"
}
```

---

### 3-5. 트랙 2 객관식 채점 기준

| 문항 | 주제 | 최상(D) | 우수(C) | 보통(B/E) | 미흡(E/B) | 최저(A) |
| --- | --- | --- | --- | --- | --- | --- |
| Q1 | AI 오류 대응 | 도구분리형 | 로직검증형 | 티칭형 | 수작업형 | 재촉형 |
| Q2 | 반복 작업 효율화 | 시스템구축형 | 샘플활용형 | 가이드추각형 | 보조활용형 | 단순반복형 |
| Q3 | 전략 기획 접근 | 프로세스설계형 | 역할부여형 | 검토비판형 | 맥락제공형 | 결과중심형 |
| Q4 | AI 거부 우회 | 프레임전환형 | 상황극형 | 대상확장형 | 도구전환형 | 재요청형 |

---

### 3-6. 트랙 2 인덱스

```sql
CREATE INDEX idx_track2_grade       ON track2_results(grade);
CREATE INDEX idx_track2_total_score ON track2_results(total_score);
CREATE INDEX idx_track2_created_at  ON track2_results(created_at);
```

---

## 4. AI 코딩 어시스턴트를 위한 개발 가이드

> Claude Code 또는 Codex에 이 문서를 컨텍스트로 제공할 때 함께 전달하세요.
> 

### 4-1. 백엔드 구현 시 주의사항

**채점 로직**

- 객관식 점수(mc)와 줄글 점수(prompt)를 반드시 분리해서 저장
- 최종 점수 = mc × 0.4 + prompt × 0.6 공식 적용
- 객관식 원본 답변은 저장
- Track 2 freeText 원문은 저장하지 않음
- LLM raw output 전체도 운영 DB에는 저장하지 않음
- 개발 중 디버깅이 필요할 때만 임시 로그로 확인하고 배포 전 제거

**LLM 연동**

- 트랙 1: 외부 LLM(유저 AI) → 결과 JSON을 프론트가 받아서 전송
- 트랙 2: 백엔드에서 LLM 직접 호출하여 줄글 채점
- 트랙 2 freeText 원문은 백엔드에서 분석 후 폐기
- LLM 원본 출력은 운영 DB에 저장하지 않음
- 필요한 경우 개인정보가 제거된 extracted_features, prompt_scores, feedback만 저장

**에러 처리**

- LLM JSON 파싱 실패 시 → `status: error` 반환, DB 저장 안 함
- 트랙 1에서 `evidence_mode: minimal` 이면 결과 신뢰도 낮음 표시

### 4-2. 추천 기술 스택

| 역할 | 추천 |
| --- | --- |
| 프론트 | GitHub Pages |
| DB | Supabase |
| 백엔드 | Supabase Edge Functions |
| 인증 | 로그인 없음, respondent_id + access_token 사용 |
| LLM 연동 | Anthropic API / OpenAI API |

### 4-3. API 엔드포인트 구조 (예시)

```
POST /api/track1/evaluate          → 트랙 1 결과 제출 및 채점
POST /api/track2/evaluate          → 트랙 2 결과 제출 및 채점

GET /api/results/track1/:shareSlug → 트랙 1 공유 결과 조회
GET /api/results/track2/:shareSlug → 트랙 2 공유 결과 조회
```

---

## 5. 공통 결과 테이블 (respondent_summary)

> MVP에서는 `respondent_summary` 테이블을 사용하지 않습니다.
> 
> 
> 트랙 1, 2의 최종 결과는 각각 `track1_results`, `track2_results`에 저장합니다.
> 
> 분석·집계·대시보드가 필요해질 때 추후 View 또는 별도 summary 테이블로 확장합니다.
> 

---

### 5-1. 테이블 정의

MVP에서는 respondent_summary 테이블을 생성하지 않습니다.

이유는 다음과 같습니다.

```
1. track1_results, track2_results에 이미 최종 결과가 저장된다.
2. summary 테이블을 따로 두면 중복 저장과 동기화 문제가 생긴다.
3. 처음 개발하는 팀 기준으로 upsert 로직이 복잡해진다.
4. MVP에서는 결과 저장, 결과 조회, 공유 링크 구현이 우선이다.
5. 통계/대시보드가 필요해질 때 추후 View 또는 별도 summary 테이블로 확장한다.
```

---

### 5-2. JSON Schema

MVP에서는 respondent_summary JSON Schema를 사용하지 않습니다.

대신 최종 DB 구조는 아래 4개 테이블만 사용합니다.

```
respondents
- 로그인 없는 닉네임 기반 임시 응시자 테이블

track1_results
- Track 1 상세 결과 테이블

track2_results
- Track 2 상세 결과 테이블

diagnosis_answers
- Track 1, Track 2 문항별 객관식 답변 공통 저장 테이블
```

---

### 5-3. 인덱스

MVP에서는 respondent_summary 관련 인덱스를 생성하지 않습니다.

사용하지 않는 인덱스:

```sql
-- 사용하지 않음
CREATE INDEX idx_summary_track1_type   ON respondent_summary(track1_type_code);
CREATE INDEX idx_summary_track2_grade  ON respondent_summary(track2_grade);
CREATE INDEX idx_summary_birth_year    ON respondent_summary(birth_year);
CREATE INDEX idx_summary_created_at    ON respondent_summary(created_at);
```

MVP에서 사용하는 주요 인덱스는 다음과 같습니다.

```sql
CREATE INDEX idx_track1_type_code  ON track1_results(type_code);
CREATE INDEX idx_track1_created_at ON track1_results(created_at);

CREATE INDEX idx_track2_grade       ON track2_results(grade);
CREATE INDEX idx_track2_total_score ON track2_results(total_score);
CREATE INDEX idx_track2_created_at  ON track2_results(created_at);
```

`share_slug`는 각 결과 테이블에서 `unique`로 설정하므로 별도 인덱스를 추가하지 않습니다.

---

### 5-4. 전체 테이블 관계도

MVP 전체 테이블 관계도는 다음과 같습니다.

```
respondents
   ├── track1_results
   │       └── diagnosis_answers
   │
   └── track2_results
           └── diagnosis_answers
```

관계 의미는 다음과 같습니다.

```
respondents
= 로그인 없는 임시 응시자

track1_results
= Track 1 최종 결과

track2_results
= Track 2 최종 결과

diagnosis_answers
= Track 1, Track 2 문항별 원본 답변
```

저장 흐름은 다음과 같습니다.

```
유저가 사이트 접속
        ↓
닉네임, 성별, 출생년도 입력
        ↓
respondents 생성
        ↓
respondent_id와 access_token을 localStorage에 저장
        ↓
Track 1 또는 Track 2 선택
```

Track 1 저장 흐름은 다음과 같습니다.

```
유저가 Track 1 완료
        ↓
Supabase Edge Function으로 제출
        ↓
respondent_id + access_token 검증
        ↓
track1_results에 최종 결과 저장
        ↓
diagnosis_answers에 Q1~Q12 답변 저장
        ↓
share_slug 반환
        ↓
결과 화면 표시
```

Track 2 저장 흐름은 다음과 같습니다.

```
유저가 Track 2 완료
        ↓
Supabase Edge Function으로 제출
        ↓
respondent_id + access_token 검증
        ↓
백엔드에서 LLM feature extraction 수행
        ↓
Track 2 freeText 원문은 저장하지 않고 폐기
        ↓
track2_results에 최종 결과 저장
        ↓
diagnosis_answers에 Q1~Q4 답변 저장
        ↓
share_slug 반환
        ↓
결과 화면 표시
```

트랙 간 연결은 `nickname + birth_year` 조합으로 하지 않습니다.

이미 `respondents` 테이블이 있으므로 `respondent_id`를 기준으로 연결합니다.

핵심 연결 키는 다음과 같습니다.

```
respondents.id
→ track1_results.respondent_id
→ track2_results.respondent_id
→ diagnosis_answers.respondent_id
```

그리고 `diagnosis_answers.result_id`는 Track에 따라 아래 둘 중 하나를 가리킵니다.

```
track = "track1"이면 track1_results.result_id
track = "track2"이면 track2_results.result_id
```

DB 차원에서 `diagnosis_answers.result_id`를 두 테이블에 동시에 FK로 걸기는 애매하므로, MVP에서는 FK 제약 없이 저장하고 `track + result_id` 조합으로 해석하는 방식을 사용합니다.

---

### 5-5. 활용 예시 쿼리

MVP에서는 respondent_summary를 사용하지 않으므로, 아래 쿼리는 각 결과 테이블 기준으로 실행합니다.

**트랙 1 유형별 분포**

```sql
SELECT type_name, COUNT(*) as count
FROM track1_results
WHERE status = 'success'
GROUP BY type_name
ORDER BY count DESC;
```

**트랙 2 등급별 분포**

```sql
SELECT grade, COUNT(*) as count
FROM track2_results
WHERE status = 'success'
GROUP BY grade
ORDER BY count DESC;
```

**트랙 2 평균 점수**

```sql
SELECT AVG(total_score) AS avg_track2_score
FROM track2_results
WHERE status = 'success';
```

**컨퍼런스 발표용 — 전체 현황**

```sql
SELECT
  (SELECT COUNT(*) FROM respondents) AS total_respondents,
  (SELECT COUNT(*) FROM track1_results WHERE status = 'success') AS track1_count,
  (SELECT COUNT(*) FROM track2_results WHERE status = 'success') AS track2_count,
  (SELECT AVG(total_score) FROM track2_results WHERE status = 'success') AS avg_track2_score;
```